# PyTorch's official CPU-only package index. Install this file before the main
# requirements so dependency resolution cannot select CUDA-enabled Linux wheels.
--index-url https://download.pytorch.org/whl/cpu
torch==2.12.1
