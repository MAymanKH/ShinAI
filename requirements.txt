# Runtime dependencies live in pyproject.toml -- this file only adds the pip
# flags that pyproject cannot express.
#
#   pip install -r requirements.txt
#
# The torch==2.12.1+cpu pin needs PyTorch's own wheel index; without it pip
# resolves the CUDA-enabled Linux wheels from PyPI instead.
--extra-index-url https://download.pytorch.org/whl/cpu
-e .
